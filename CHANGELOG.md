## 0.0.1

* Initial release.
* iOS: `JoinInfo.screenShareExtensionId` and the `ChimeScreenShareExtension` Info.plist key can now override the default `<mainBundleId>.ScreenShareExt` suffix so the broadcast upload extension target can be named anything.
